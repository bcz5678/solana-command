import { requireSuperAdmin } from "@/lib/auth/require-super-admin"
import { getBuySolAmountFromTokenAmount } from "@pump-fun/pump-sdk"
import { publicDecrypt } from "crypto"
import { request } from "http"
import { connection } from "next/server"

POST request
QN connection
const executor = new Executor(connection, wallet, default slippage: .05, maxRetries: );

requireSuperAdmin

wallet = requireSuperAdmin.rpc('get_wallet_by_id)

    await    executor.buy(mint)

    wallet.secretKey.fill(0)






